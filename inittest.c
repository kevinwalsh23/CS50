#define _XOPEN_SOURCE 500

#include <cs50.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int d;
int board[DIM_MAX][DIM_MAX];
// constants
#define DIM_MIN 3
#define DIM_MAX 9

int main(int argc, string argv[])
{

int init [d] [d]
{
   for (int i = 0; i<d; i++)
        for (int k = 0; k<d; k++)
        {
            [i] [k] = (d*d) -1
        }
   
   
    // TODO
    
}